# Entornos y dependencias
venv/
__pycache__/
*.pyc
node_modules/

# Datos y estado local (no es código, es lo que generan las apps al correr)
*.db
*.sqlite3
.env
.clave_sesion

# Contactos/datos reales de terceros — nunca al repo público (es público en GitHub)
freelancer/clientes-freelance/privado/

# Logs y cachés
*.log
.pytest_cache/
